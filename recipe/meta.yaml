{% set name = "Flask-Security-Too" %}
{% set version = "5.0.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: a4e49bd31aaf08dd451d0153802a42fdb0a5f522fa3c9593ab114dabb2ddd815

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - flask >=2.0,<2.3
    - flask-login >=0.6
    - flask-principal >=0.4.0
    - flask-wtf >=1.0.0
    - email-validator >=1.1.1
    - itsdangerous >=2.0.0
    - passlib >=1.7.2
    - blinker >=1.4
    - wtforms >=3.0.0

test:
  imports:
    - flask_security
    - flask_security.models
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Flask-Middleware/flask-security
  summary: Simple security for Flask apps.
  license: MIT
  license_file: LICENSE
  doc_url: https://flask-security-too.readthedocs.io/

extra:
  recipe-maintainers:
    - synapticarbors
